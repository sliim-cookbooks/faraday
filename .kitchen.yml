---
driver_plugin: vagrant
driver_config:
  require_chef_omnibus: true

platforms:
- name: debian-7.6

suites:
- name: faraday
  run_list:
  - recipe[couchdb]
  - recipe[faraday]
  attributes:
    couchdb:
      httpd:
        bind_address: 127.0.0.1
        database_dir: /var/lib/couchdb
    faraday:
      config:
        user: vagrant
